# Serveur
PORT=5000
NODE_ENV=development

# Base de données (décommentez la ligne que vous utilisez)
# MongoDB local (par défaut)
#MONGODB_URI=mongodb://localhost:27017/concours_db
# OU MongoDB Atlas
 MONGODB_URI=mongodb+srv://zobridel_db_user:yt53L8XgHCv7s3P9cluster@cluster0.mongodb.net/concours_db?retryWrites=true&w=majority

# JWT (gardez la même clé que dans votre middleware)
JWT_SECRET=votre_cle_secrete_tres_longue_et_complexe_123456789

# Options CORS (si nécessaire)
CORS_ORIGIN=http://localhost:3000,http://localhost:5173